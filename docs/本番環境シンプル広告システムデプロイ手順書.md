# 本番環境シンプル広告システムデプロイ手順書
**日時**: 2025-08-21  
**対象**: 本番環境(qgqcjtjxaoplhxurbpis)  
**目的**: 必要最小限の広告システムテーブルのみを本番環境に展開

## 事前準備

### 1. データバックアップ
```bash
# 本番環境全体のバックアップ取得
npx supabase db dump --project-ref qgqcjtjxaoplhxurbpis --file production_backup_$(date +%Y%m%d_%H%M%S).sql

# 広告関連テーブルのみバックアップ
npx supabase db dump --project-ref qgqcjtjxaoplhxurbpis --table advertisers --table ad_campaigns --table ad_creatives --table ad_placements --table ad_flights --table ad_events --file production_ad_tables_backup_$(date +%Y%m%d_%H%M%S).sql
```

### 2. 既存データのエクスポート（必要な場合）
- 重要な広告主データの手動エクスポート
- 配置設定情報の記録
- 過去の広告配信データのアーカイブ

## 本番環境デプロイ手順

### ステップ1: 最小構成システムの展開
```bash
# 本番環境プロジェクトID: qgqcjtjxaoplhxurbpis
npx supabase db push --project-ref qgqcjtjxaoplhxurbpis
```

または、マイグレーションファイル実行:
- `20250821_production_minimal_ad_system.sql`
- `20250821_140000_add_battle_list_periodic_ad_placements.sql` （3件ごと配置追加）
- `20250821_140100_assign_ads_to_periodic_placements.sql` （広告割り当て）

### ステップ2: 旧システムのクリーンアップ（慎重に実行）
```bash
# 開発環境で検証済みのクリーンアップSQL実行
# ⚠️ 不可逆的操作のため十分注意
```

マイグレーション実行順序:
1. `20250821_cleanup_old_ad_tables.sql` 
2. データ整合性の確認
3. Edge Function `ad-serve` のデプロイ

### ステップ3: Edge Function デプロイ
```bash
npx supabase functions deploy ad-serve --project-ref qgqcjtjxaoplhxurbpis
```

### ステップ4: 基本データのシード
```sql
-- 必要最小限の広告主データ投入
-- 基本配置場所の設定
-- 初期広告コンテンツの投入（必要に応じて）
```

## 動作確認項目

### 1. テーブル構成確認
- ✅ advertisers
- ✅ ad_placements (11個の配置: after-3,6,9,12,15,18,21,24,27,30 + ranking.top等)
- ✅ simple_ads
- ✅ ad_placement_assignments
- ❌ ad_campaigns (削除済み)
- ❌ ad_creatives (削除済み)
- ❌ ad_flights (削除済み)
- ❌ ad_events (削除済み)

### 2. API動作確認
```bash
# 基本的な配置の確認
curl -X POST "https://qgqcjtjxaoplhxurbpis.supabase.co/functions/v1/ad-serve" \
  -H "Content-Type: application/json" \
  -d '{"placement": "ranking.top.banner"}'

# 3件ごと配置の確認
curl -X POST "https://qgqcjtjxaoplhxurbpis.supabase.co/functions/v1/ad-serve" \
  -H "Content-Type: application/json" \
  -d '{"placement": "battles.list.after-6.infeed"}'

curl -X POST "https://qgqcjtjxaoplhxurbpis.supabase.co/functions/v1/ad-serve" \
  -H "Content-Type: application/json" \
  -d '{"placement": "battles.list.after-9.infeed"}'
```

期待レスポンス:
```json
{
  "ok": true,
  "data": {
    "placement_key": "ranking.top.banner",
    "creative": {
      "creative_id": "uuid",
      "headline": "広告タイトル",
      "body": "広告説明",
      "cta_text": "詳しく見る",
      "target_url": "https://example.com",
      "file_url": "https://example.com/image.jpg"
    },
    "token": null
  }
}
```

### 3. 管理UI確認
- 広告一覧の表示
- 新規広告作成機能
- 配置設定変更機能

## ロールバック手順（緊急時）

### 1. データベースロールバック
```bash
# バックアップからの復元
psql -h db.xxx.supabase.co -U postgres -d postgres < production_backup_YYYYMMDD_HHMMSS.sql
```

### 2. Edge Function ロールバック
```bash
# 旧バージョンのEdge Function再デプロイ
npx supabase functions deploy ad-serve --project-ref qgqcjtjxaoplhxurbpis
```

## 削減効果

### テーブル数削減
- **削除前**: 6テーブル (advertisers, ad_campaigns, ad_creatives, ad_placements, ad_flights, ad_events)
- **削除後**: 4テーブル (advertisers, ad_placements, simple_ads, ad_placement_assignments)
- **削減率**: 33%削減

### 複雑度削減
- キャンペーン・フライト管理の撤廃
- 計測システムの一時停止
- ターゲティングロジックの簡略化
- 優先度ベース配信への変更

## 注意事項

1. **不可逆的操作**: 旧テーブル削除は元に戻せません
2. **データ移行**: 既存広告データは手動移行が必要
3. **権限設定**: RLS ポリシーで適切なアクセス制御が設定済み
4. **監視**: 本番デプロイ後24時間は動作を密に監視

## 問題発生時の連絡先

- **技術責任者**: [担当者名]
- **エスカレーション先**: [管理者連絡先]
- **緊急時対応**: [緊急連絡先]

---
**実行者**: ___________  
**実行日時**: ___________  
**確認者**: ___________
