# オンボーディング Get Started スライド Vote ボタンサイズ調整ログ

## 日付
2025-08-17

## 目的
Vote ボタンのサイズが Start battle ボタンより小さく視認性に差があったため、より大きくしバランスを調整。

## 変更ファイル
- `src/components/onboarding/slides/GetStartedSlide.tsx`

## 変更内容
- Vote ボタンのクラスを `p-2` から `p-7` へ拡大し、Start battle ボタンと統一感のある `bg-black`, `shadow-lg`, `filter brightness-110` を付与。
- 画像サイズを `w-16 h-16` から `w-14 h-14` に調整しパディング増加とのバランス最適化。

## 動作確認
- 表示崩れなし。
- Start battle ボタンとの視覚的バランス改善。
- 型エラーなし。

## 今後の検討
- モバイル表示での2カラム幅余白再調整が必要ならレスポンシブクラス追加を検討。

## 完了ステータス
Done
